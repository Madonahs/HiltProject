<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.madonasyombua.learninghilt">
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <application
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LearningHilt">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

      <!--  <intent-filter>
            <data android:scheme="https" android:host="*.swac.demo.streem.cloud"/>
            <data android:scheme="https" android:host="*.swac.dev.streem.cloud"/>
            <data android:scheme="https" android:host="*.swac.prod-us.streem.cloud"/>
            <data android:scheme="https" android:host="*.swac.qa.streem.cloud"/>
            <data android:scheme="https" android:host="*.swac.sandbox.streem.cloud"/>
            <data android:scheme="https" android:host="*.swac.streem.team"/>
        </intent-filter>-->
    </application>


</manifest>